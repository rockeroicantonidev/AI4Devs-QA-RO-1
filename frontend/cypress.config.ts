export default {
  e2e: {
    specPattern: 'cypress/integration/**/*.js', // Permite que Cypress detecte los tests en integration
    setupNodeEvents(on, config) {
      // implement node event listeners here
    },
  },
};
